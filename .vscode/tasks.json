{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "npm",
			"script": "build",
			"group": {
				"kind": "build",
				"isDefault": true
			},
			"label": "npm: build",
			"detail": "vite build",
			// Useful for nvm, takes longer since
			// it has to run the user profile scripts
			"options": {
				"shell": {
					"args": [
						"-i"
					]
				}
			}
		}
	]
}